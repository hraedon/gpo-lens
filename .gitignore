# --- data / samples: NEVER commit (real domain SYSVOL + GPO exports live here) ---
samples/
*.zip

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.coverage
htmlcov/

# local store / generated
*.sqlite3
*.db

# secrets
.env
.env.local
.env.*.local
*.pem
*.key
.envrc
.direnv/

# local denylist for the pre-commit identifier gate (the forbidden work-domain
# identifiers themselves — must never be committed)
.identifiers-denylist.local
.denylist-review.local

# local store / generated (WAL/SHM sidecars)
*.sqlite3-wal
*.sqlite3-shm

# editor / IDE / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# editor / tooling
.claude/

# agent-notes breadcrumb export-index (regenerated; a committed copy goes stale)
OPEN_BREADCRUMBS.txt

# plan scratch space
plans/design/

# Pester -CI output
testResults.xml

# Temporary holding pen for work items that could not be filed in regista
# (e.g. CLI write blocked by a pending schema migration). Transient — drain
# into regista once writes are unblocked, then delete. Never the source of truth.
PENDING-REGISTA-WI.md
